# Premier-League-Tracker-Python-CLI-Python-REST-API-Requests
Built a terminal-based Python app that pulls live Premier League data (league table, fixtures and scorers) from a football API and displays it in a clean, readable format. Implemented an API wrapper with authentication and error handling, plus a simple menu system and flexible team-name input.
